@model ErrorViewModel
@{
    ViewData["Title"] = "Greška"; // Prevedeno
}

<div class="container text-center py-5">
    <h1 class="display-4 text-danger mb-3">Greška!</h1> @* Prevedeno i istaknuto *@
    <h2 class="text-secondary mb-4">Došlo je do greške prilikom obrade vašeg zahtjeva.</h2> @* Prevedeno *@

    @if (Model.ShowRequestId)
    {
        <p class="lead">
            <strong>ID zahtjeva:</strong> <code>@Model.RequestId</code> @* Prevedeno *@
        </p>
    }

    <hr class="my-4"> @* Dodata horizontalna linija *@

    <div class="alert alert-warning text-start" role="alert">
        <h3 class="alert-heading"><i class="bi bi-tools me-2"></i>Razvojni režim</h3> @* Prevedeno i dodata ikona *@
        <p>
            Prelazak na **razvojno okruženje** (`Development`) prikazat će detaljnije informacije o grešci koja se dogodila.
        </p>
        <p class="mb-0">
            **Razvojno okruženje se ne smije omogućiti za produkcijske aplikacije.**
            To može dovesti do prikazivanja osjetljivih informacija iz izuzetaka krajnjim korisnicima.
            Za lokalno otklanjanje grešaka (debugiranje), omogućite **razvojno okruženje** tako što ćete postaviti varijablu okruženja **`ASPNETCORE_ENVIRONMENT`** na **`Development`**
            i ponovo pokrenuti aplikaciju.
        </p>
    </div>
</div>